Neocities free tier blocks direct MP3 uploads.

Store channel audio externally (Internet Archive recommended) and place URLs in `channels.json`:

- `audio.primaryUrl` (OGG preferred)
- `audio.fallbackUrl` (MP3 fallback)

Keep this directory for optional local preview assets only.
